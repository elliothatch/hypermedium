<div class="container">Links:
	<ul>
		{{#each _links}}
		<li>{{@key}}<ul>
				{{#if (isArray this)}}
				{{#each this}}
				<li>{{hal-link @../key this}}</li>
				{{/each}}
				{{else}}
				<li>{{hal-link @key this}}</li>
				{{/if}}
		</li></ul>
		{{/each}}
	</ul>
	{{#each this}}
	<div>
		{{#unless (startsWith @key '_')}}
			<h2>{{@key}}</h2>
			{{#if (eq (typeof this) 'object')}}
				{{> core/includes/json-object.hbs this}}
			{{else}}
				<div>{{this}}</div>
			{{/if}}
		{{/unless}}
	</div>
	{{/each}}
</div>
