<h1>{{title}}</h1>
<ol class="tags">
	{{#each _embedded.fs:entries}}
	<li>
		<h2><a href="{{html-uri _links.self.href}}">{{title}}</a></h2>
		<ol class="articles">
			{{#each _embedded.fs:entries}}
			<li><a href="{{html-uri _links.self.href}}">{{title}}</a>{{#if date-created}} - {{datetime date-created 'MMMM D, YYYY'}}{{/if}}</li>
			{{/each}}
		</ol>
	</li>
	{{/each}}
</ol>
